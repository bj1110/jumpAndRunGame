{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake Build",  // <== This must match launch.json
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "build/"
            ],
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
